import numpy as np 
def get_array_properties(arr): 
    ''' INPUT: arr -> A nD array OUTPUT: result -> tuple consisting of shape and dimension ''' 
    ## STEP 1. Get the shape of array 
    
    
    ## STEP 2. Get the dimension of array 
    
    
    return shape,dim

get_array_properties(np.array([0, 1, 2, 3, 4, 5]))